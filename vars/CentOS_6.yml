---

# pkg-mirror packages
pkg_mirror_packages:
  - epel-release

# pkg-mirror
pkg_mirror_url_list_default:
  - name: base
    file: CentOS-Base
    description: CentOS-$releasever - Base
    baseurl: https://pkg.adfinis.com/centos/$releasever/os/$basearch
    gpgcheck: true
    repo_gpgcheck: true
    gpgkey: file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-6
